streamlit>=1.32.0
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0
joblib>=1.3.0

# Pick ONE of the following deep learning backends:
tensorflow>=2.13.0       # recommended (full LSTM)
keras>=3.0.0             # fallback backend when TensorFlow wheel is unavailable
# torch>=2.0.0           # alternative (not used directly but easy to swap in)

# Optional: only needed if you add plotting beyond st.line_chart
matplotlib>=3.7.0
plotly>=5.18.0
